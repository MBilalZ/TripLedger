{
  "name": "tripledger",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter @tripledger/web dev --host",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:engine": "pnpm --filter @tripledger/engine test",
    "setup:supabase": "node scripts/setup-supabase.mjs"
  },
  "devDependencies": {
    "pg": "^8.14.1"
  },
  "engines": {
    "node": ">=18"
  }
}
