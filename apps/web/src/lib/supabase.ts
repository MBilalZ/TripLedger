/** @deprecated Import from `@/api/supabase` or `@/api` instead. */
export {
  isSupabaseConfigured,
  getSupabase,
  ensureAuthSession,
  requireUser,
  currentUserId,
  signUpWithPassword,
  signInWithPassword,
  signOut,
} from "@/api/supabase";
