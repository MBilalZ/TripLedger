/** @deprecated Import from `@/api/supabase` or `@/api` instead. */
export {
  currentUserId,
  ensureAuthSession,
  getSupabase,
  isSupabaseConfigured,
  requireUser,
  signInWithPassword,
  signOut,
  signUpWithPassword,
} from "@/api/supabase";
