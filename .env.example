# Copy to gitignored `.env.supabase` at repo root, then:
#   pnpm setup:supabase
#
# Never commit real keys.

SUPABASE_PROJECT_REF=whpcaifbxqkcoejscxzs
SUPABASE_ANON_KEY=
SUPABASE_DB_PASSWORD=
SUPABASE_SERVICE_ROLE_KEY=
# Optional — Account → Access Tokens (email auth autoconfirm + redirect URLs via API)
SUPABASE_ACCESS_TOKEN=
